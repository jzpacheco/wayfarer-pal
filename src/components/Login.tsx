// src/components/Login.tsx
import React from 'react';

const Login: React.FC = () => {
  // Lógica de login pode ser implementada aqui

  return (
    <div className="login">
      <h2>Login</h2>
      {/* Campos de login, formulário, etc. */}
    </div>
  );
};

export default Login;
